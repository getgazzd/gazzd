import React from "react";
import { Product } from "types";

interface Props {
  product: Product;
}

const ProductStock = ({ product }: Props) => {
  return <div>ProductStock</div>;
};

export default ProductStock;
